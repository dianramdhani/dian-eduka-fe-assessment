const state = () => {
    return {
        duration: 0,
        leftTime: 0,
        inProgress: false
    };
};

const getters = {};

const actions = {};

const mutations = {};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
}