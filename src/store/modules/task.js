const state = () => {
    return {
        questions: [],
        userAnswers: [],
        keyAnswers: [],
        currentNumber: 0
    };
};

const getters = {};

const actions = {};

const mutations = {};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
}